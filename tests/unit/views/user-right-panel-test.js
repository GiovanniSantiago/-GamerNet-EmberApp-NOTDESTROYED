import { moduleFor, test } from 'ember-qunit';

moduleFor('view:user-right-panel', 'Unit | View | user right panel');

// Replace this with your real tests.
test('it exists', function(assert) {
  var view = this.subject();
  assert.ok(view);
});
